import Quick
import Nimble

final class WiringSpec: QuickSpec {
    override func spec() {
        describe("wiringTest") {
            it("it just works") {
                expect(true).to(beTrue())
            }
        }
    }
}
